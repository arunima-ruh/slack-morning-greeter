# Slack Morning Greeter — Environment Configuration

# ── Slack Configuration ───────────────────────────────────────────────────────
SLACK_BOT_TOKEN=xoxb-your-bot-token-here
SLACK_USER_ID=U01234567

# ── Database Configuration (Optional) ─────────────────────────────────────────
# The agent works without a database, but won't track delivery history
PG_CONNECTION_STRING=postgresql://user:password@localhost:5432/openclaw
ORG_ID=default
AGENT_ID=slack-morning-greeter
